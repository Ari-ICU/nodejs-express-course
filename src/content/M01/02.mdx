export const meta = {
  id: "M01-02",
  number: "02",
  title: "Frontend vs Backend",
  khmerTitle: "ភាពខុសគ្នារវាង Frontend និង Backend",
  type: "concept",
  imageUrl: "/images/lessons/frontend-vs-backend.png",
  summary: `**Frontend** — executes inside the user's browser:
- Built with HTML, CSS, JavaScript (React, Next.js, Vue)
- Renders user interfaces, animations, and form inputs
- Manages client-side state and browser cookies
- Sends requests to the backend via HTTP/REST APIs
- Has **zero privacy** — all source code is readable by users

**Backend** — runs on a secure, private server:
- Built with Node.js, Express, databases (MongoDB, PostgreSQL)
- Enforces authentication, authorization, and data validation
- Executes database reads/writes with ACID transactions
- Safely protects private API secrets and payment keys
- Transmits sanitized JSON responses back to the client`,
  tip: "Never store private API keys, database credentials, or secret hashing salts on the frontend.",
  objective: "Differentiate client execution boundaries from server execution boundaries.",
  expectedOutcome: "Confident identification of which responsibilities belong to the server versus the client.",
};
