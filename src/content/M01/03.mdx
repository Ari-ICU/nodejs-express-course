export const meta = {
  id: "M01-03",
  number: "03",
  title: "Client-Server Architecture",
  khmerTitle: "ស្ថាបត្យកម្ម Client-Server",
  type: "architecture",
  imageUrl: "/images/lessons/client-server-architecture.png",
  summary: `**Client-Server Architecture** is the foundational distributed computing model powering the modern internet:
- **Clients (Frontend Devices)**: Web browsers, mobile apps, and IoT devices initiate requests over network protocols (HTTP/HTTPS, TCP/IP).
- **Servers (Backend Hosts)**: Centralized Node.js + Express servers authenticate requests, enforce business logic, and compute responses.
- **Databases & Caches**: High-performance persistence layers (MongoDB, PostgreSQL) and in-memory caches (Redis) supply real-time data.
- **Stateless Communication**: In REST APIs, each request contains complete authentication credentials (JWT Bearer tokens) to operate independently.`,
  tip: "Statelessness is key in modern REST: each HTTP request must carry all authentication metadata necessary to fulfill it independently.",
  objective: "Trace the path of an HTTP packet from client interaction to database query and response.",
  expectedOutcome: "Deep comprehension of reverse proxies, application servers, and persistence layers.",
};
