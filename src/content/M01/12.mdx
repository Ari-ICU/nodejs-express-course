export const meta = {
  id: "M01-12",
  number: "12",
  title: "Node.js Project Structure",
  khmerTitle: "រចនាសម្ព័ន្ធ Folder គម្រោង Node.js",
  type: "architecture",
  codeLanguage: "bash",
  summary: "A clean Node.js project starts with a predictable layout: keeping source files inside `src/`, isolating the entrypoint (`index.js`), tracking dependencies in `package.json`, and documenting setup steps in `README.md`.",
  tip: "Always organize application source code inside a dedicated `src/` directory rather than placing multiple loose scripts in the root folder.",
  objective: "Bootstrap a clean, maintainable directory structure for a foundational Node.js project.",
  expectedOutcome: "Clear mental model of standard project files and folders in a Node.js repository.",
  codeSnippet: "my-node-app/\n├── src/\n│   ├── index.js      # Main application script & entrypoint\n│   └── utils.js      # Helper functions & reusable utilities\n├── .gitignore        # Excludes node_modules and sensitive files\n├── README.md         # Project documentation and instructions\n└── package.json      # Project metadata, scripts, and dependencies",
};

```bash
my-node-app/
├── src/
│   ├── index.js      # Main application script & entrypoint
│   └── utils.js      # Helper functions & reusable utilities
├── .gitignore        # Excludes node_modules and sensitive files
├── README.md         # Project documentation and instructions
└── package.json      # Project metadata, scripts, and dependencies
```
