export const meta = {
  id: "M06-14",
  number: "14",
  title: "Express Project Structure",
  khmerTitle: "រចនាសម្ព័ន្ធគម្រោង Express.js កម្រិតមូលដ្ឋាន",
  type: "architecture",
  codeLanguage: "bash",
  summary: "An idiomatic Express project separates the Express application configuration (`app.js`) from the server network listener (`server.js`). This makes the codebase modular, clean, and ready for future routing modules.",
  tip: "Exporting `app` from `app.js` without immediately calling `.listen()` in the same file keeps the application modular and easily testable.",
  objective: "Organize an Express project into clean, decoupled application and server files.",
  expectedOutcome: "Clean project layout separating app setup from the network port listener.",
  codeSnippet: "my-express-app/\n├── public/           # Static assets (images, stylesheets, html)\n├── src/\n│   ├── app.js        # Express app configuration & middleware setup\n│   └── server.js     # Entrypoint: listens on PORT & boots the server\n├── .env              # Environment variables (e.g. PORT=5000)\n├── .gitignore        # Excludes node_modules & sensitive files\n├── package.json      # Express dependency & npm start/dev scripts\n└── README.md         # Project instructions",
};

```bash
my-express-app/
├── public/           # Static assets (images, stylesheets, html)
├── src/
│   ├── app.js        # Express app configuration & middleware setup
│   └── server.js     # Entrypoint: listens on PORT & boots the server
├── .env              # Environment variables (e.g. PORT=5000)
├── .gitignore        # Excludes node_modules & sensitive files
├── package.json      # Express dependency & npm start/dev scripts
└── README.md         # Project instructions
```
