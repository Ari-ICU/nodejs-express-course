export const meta = {
  id: "M09-09",
  number: "09",
  title: "PATCH",
  khmerTitle: "ការអនុវត្ត PATCH Endpoint ស្តង់ដារ",
  type: "lab",
  codeLanguage: "javascript",
  summary: "Building PATCH endpoints that update only the provided fields in the request payload while preserving all other existing resource attributes.",
  tip: "PATCH updates only specified fields, whereas PUT completely replaces the resource representation.",
  objective: "Implement partial resource updates.",
  expectedOutcome: "Flexible partial updates without unintentional data erasure.",
  codeSnippet: "app.patch(\"/api/v1/products/:id\", (req, res) => {\n  const product = products.find((p) => p.id === req.params.id);\n  if (!product) {\n    return res.status(404).json({ success: false, error: \"Product not found\" });\n  }\n\n  // Partial update: merge incoming fields over existing attributes\n  Object.assign(product, req.body);\n\n  res.json({ success: true, data: product });\n});",
};

```javascript
app.patch("/api/v1/products/:id", (req, res) => {
  const product = products.find((p) => p.id === req.params.id);
  if (!product) {
    return res.status(404).json({ success: false, error: "Product not found" });
  }

  // Partial update: merge incoming fields over existing attributes
  Object.assign(product, req.body);

  res.json({ success: true, data: product });
});
```
