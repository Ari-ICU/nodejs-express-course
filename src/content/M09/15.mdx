export const meta = {
  id: "M09-15",
  number: "15",
  title: "Filtering",
  khmerTitle: "ការបង្កើត Query Filtering សម្រាប់ Search & Categories",
  type: "lab",
  codeLanguage: "javascript",
  summary: "Filtering allows clients to query resources matching specific criteria (e.g. `?category=electronics&price[gte]=100&inStock=true`).",
  tip: "Always convert and validate query string types (such as parsing numeric values with `Number()`) before comparing values.",
  objective: "Build flexible query filters supporting ranges and categories.",
  expectedOutcome: "Flexible collection filtering supporting multiple criteria.",
  codeSnippet: "app.get(\"/api/v1/products\", (req, res) => {\n  let results = [...products];\n\n  if (req.query.category) {\n    results = results.filter((p) => p.category === req.query.category);\n  }\n\n  if (req.query.minPrice) {\n    const min = Number(req.query.minPrice);\n    results = results.filter((p) => p.price >= min);\n  }\n\n  res.json({ success: true, count: results.length, data: results });\n});",
};

```javascript
app.get("/api/v1/products", (req, res) => {
  let results = [...products];

  if (req.query.category) {
    results = results.filter((p) => p.category === req.query.category);
  }

  if (req.query.minPrice) {
    const min = Number(req.query.minPrice);
    results = results.filter((p) => p.price >= min);
  }

  res.json({ success: true, count: results.length, data: results });
});
```
