export const meta = {
  id: "M09-16",
  number: "16",
  title: "Searching",
  khmerTitle: "ការបង្កើត Search Query Endpoint សម្រាប់ស្វែងរកទិន្នន័យ",
  type: "lab",
  codeLanguage: "javascript",
  summary: "Text searching allows clients to query resources using keyword search parameters (e.g. `?q=mouse`), performing case-insensitive matching across resource fields.",
  tip: "Always trim and lowercase incoming search terms to ensure consistent, case-insensitive query matching.",
  objective: "Implement keyword search across collection resources.",
  expectedOutcome: "Fast text search across product catalogs and resource lists.",
  codeSnippet: "app.get(\"/api/v1/search\", (req, res) => {\n  const query = (req.query.q || \"\").toLowerCase().trim();\n  if (!query) {\n    return res.json({ success: true, count: 0, data: [] });\n  }\n\n  // Case-insensitive substring search across collection\n  const results = products.filter(\n    (p) => p.name.toLowerCase().includes(query)\n  );\n\n  res.json({ success: true, count: results.length, data: results });\n});",
};

```javascript
app.get("/api/v1/search", (req, res) => {
  const query = (req.query.q || "").toLowerCase().trim();
  if (!query) {
    return res.json({ success: true, count: 0, data: [] });
  }

  // Case-insensitive substring search across collection
  const results = products.filter(
    (p) => p.name.toLowerCase().includes(query)
  );

  res.json({ success: true, count: results.length, data: results });
});
```
