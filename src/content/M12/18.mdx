export const meta = {
  id: "M12-18",
  number: "18",
  title: "Mongoose Methods",
  khmerTitle: "ការបង្កើត Instance Methods & Static Methods",
  type: "lab",
  codeLanguage: "javascript",
  summary: "Instance methods operate on an individual document instance (e.g. `product.applyDiscount()`), while Static methods operate across the Model collection (e.g. `Product.findByCategory()`).",
  tip: "Never use arrow functions (`() => {}`) for Mongoose methods because arrow functions do not bind `this` to the document instance.",
  objective: "Add custom domain logic directly onto Mongoose Models.",
  expectedOutcome: "Rich Domain Model encapsulation with instance and static methods.",
  codeSnippet: "// Instance method (operates on individual document instance):\nproductSchema.methods.applyDiscount = function(percent) {\n  this.price = Math.round(this.price * (1 - percent / 100));\n  return this.save();\n};\n\n// Static method (operates on model collection):\nproductSchema.statics.findByCategory = function(category) {\n  return this.find({ category });\n};",
};

```javascript
// Instance method (operates on individual document instance):
productSchema.methods.applyDiscount = function(percent) {
  this.price = Math.round(this.price * (1 - percent / 100));
  return this.save();
};

// Static method (operates on model collection):
productSchema.statics.findByCategory = function(category) {
  return this.find({ category });
};
```
