export const meta = {
  id: "M20-10",
  number: "10",
  title: "Asynchronous Email Dispatch",
  khmerTitle: "ការផ្ញើ Email ដោយមិនរាំងស្ទះ HTTP (Asynchronous Dispatch)",
  type: "lab",
  codeLanguage: "javascript",
  summary: "Decoupling email delivery from the HTTP request cycle using asynchronous background dispatch ensures sub-50ms API response times while slow SMTP handshakes complete in the background.",
  tip: "Always wrap background asynchronous tasks in try/catch error boundaries so SMTP failures are safely logged without crashing the process.",
  objective: "Implement asynchronous non-blocking email delivery in Express controllers.",
  expectedOutcome: "Fast sub-50ms API responses decoupled from slow SMTP network latency.",
  codeSnippet: "// services/emailDispatcher.js\nimport { sendEmail } from \"./email.service.js\";\n\n// Decoupled asynchronous dispatcher: runs without blocking HTTP responses\nexport const dispatchEmailAsync = (options) => {\n  setImmediate(async () => {\n    try {\n      await sendEmail(options);\n      console.log(`✉️ Email sent to ${options.to}`);\n    } catch (err) {\n      console.error(`❌ Background email failed: ${err.message}`);\n    }\n  });\n};\n\n// Inside Express Controller:\nexport const requestPasswordReset = async (req, res) => {\n  const { email } = req.body;\n\n  // Dispatch email in background without awaiting SMTP handshake\n  dispatchEmailAsync({\n    to: email,\n    subject: \"Password Reset\",\n    html: \"<p>Click here to reset your password.</p>\",\n  });\n\n  // Immediate response to user (<50ms!)\n  res.json({ success: true, message: \"Reset link sent to your email!\" });\n};",
};

```javascript
// services/emailDispatcher.js
import { sendEmail } from "./email.service.js";

// Decoupled asynchronous dispatcher: runs without blocking HTTP responses
export const dispatchEmailAsync = (options) => {
  setImmediate(async () => {
    try {
      await sendEmail(options);
      console.log(`✉️ Email sent to ${options.to}`);
    } catch (err) {
      console.error(`❌ Background email failed: ${err.message}`);
    }
  });
};

// Inside Express Controller:
export const requestPasswordReset = async (req, res) => {
  const { email } = req.body;

  // Dispatch email in background without awaiting SMTP handshake
  dispatchEmailAsync({
    to: email,
    subject: "Password Reset",
    html: "<p>Click here to reset your password.</p>",
  });

  // Immediate response to user (<50ms!)
  res.json({ success: true, message: "Reset link sent to your email!" });
};
```
