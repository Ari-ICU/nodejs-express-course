export const meta = {
  id: "M28-20",
  number: "20",
  title: "Production Checklist",
  khmerTitle: "បញ្ជីត្រួតពិនិត្យចុងក្រោយមុនពេល Launch (Production Go-Live Checklist)",
  type: "architecture",
  codeLanguage: "markdown",
  summary: "Before launching an API to real users, engineers must verify a comprehensive production checklist. This covers environment secrets validation, database indexes, rate limiting, error logging to external tools (Sentry), automated backups, SSL encryption, and health checks.",
  tip: "Always create an automated daily MongoDB backup cron job (`mongodump`) and upload archives to S3/R2 before opening your application to public users.",
  objective: "Execute a thorough production readiness audit ensuring zero launch gaps.",
  expectedOutcome: "Rock-solid, audit-verified backend ready for real-world enterprise traffic.",
  codeSnippet: "## Enterprise Production Go-Live Checklist:\n- [ ] 1. Security: Helmet headers active & X-Powered-By removed\n- [ ] 2. CORS: Explicit origin whitelisting configured (No wildcard '*')\n- [ ] 3. Secrets: All passwords & JWT secrets have 32+ bytes entropy in env\n- [ ] 4. Database: Compound indexes created & query latency verified with explain()\n- [ ] 5. Rate Limiting: Active on all endpoints, strict on auth (/login)\n- [ ] 6. Caching: Redis TTL configured on read-heavy public endpoints\n- [ ] 7. Error Handling: No internal stack traces leaked in 500 responses\n- [ ] 8. Monitoring: Health check route (/health) returning DB + Redis status\n- [ ] 9. Backups: Daily automated database snapshots to Cloud storage (R2/S3)\n- [ ] 10. SSL/TLS: Valid HTTPS certificate with auto-renewal active",
};

```markdown
## Enterprise Production Go-Live Checklist:
- [ ] 1. Security: Helmet headers active & X-Powered-By removed
- [ ] 2. CORS: Explicit origin whitelisting configured (No wildcard '*')
- [ ] 3. Secrets: All passwords & JWT secrets have 32+ bytes entropy in env
- [ ] 4. Database: Compound indexes created & query latency verified with explain()
- [ ] 5. Rate Limiting: Active on all endpoints, strict on auth (/login)
- [ ] 6. Caching: Redis TTL configured on read-heavy public endpoints
- [ ] 7. Error Handling: No internal stack traces leaked in 500 responses
- [ ] 8. Monitoring: Health check route (/health) returning DB + Redis status
- [ ] 9. Backups: Daily automated database snapshots to Cloud storage (R2/S3)
- [ ] 10. SSL/TLS: Valid HTTPS certificate with auto-renewal active
```
